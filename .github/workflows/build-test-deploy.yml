name :  Build Test Deploy
on :
   push :
        branches :
            - main

jobs :
     build:
        runs-on : ubuntu-latest
        steps:
          - name : Build
            run :  echo "Building"

     test:
        runs-on : ubuntu-latest
        needs : build
        steps:
          - name : Test
            run :  echo "Testing"
      
     deploy:
        runs-on : ubuntu-latest
        needs : test
        steps:
          - name : Deploy
            run :  echo "Deploying"
